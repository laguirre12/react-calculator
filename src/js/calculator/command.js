class Command {
  constructor() { }
  execute() {}
}

class Command {
  constructor() { }

  token() {}
  precendence() {}
  execute(a, b) {}
}

/**
 * Every command should have
 * - a single character 'token',
 * - a function operation
 * - a precedence
 */
